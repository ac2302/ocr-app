import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aryansdomain.ocrapp',
  appName: 'ocr-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
